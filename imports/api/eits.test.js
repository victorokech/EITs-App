/* eslint-env mocha */

import { Meteor } from 'meteor/meteor';

if (Meteor.isServer) {
    describe('EITS', () => {
        describe('methods', () => {
            it('can delete owned task', () => {});
        });
    });
}